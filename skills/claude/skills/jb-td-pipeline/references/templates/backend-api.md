---
ticket: {ticketId}
---

# {ticketId} 后端 API 设计

## 接口清单
| 序号 | 方法 | 路径 | 说明 | 权限 | 所属 Controller |

## 接口详情

### {接口名称}
- **路径**：`POST /api/{service}/{resource}/{action}`
- **权限**：`{权限标识}`
- **请求 DTO**：
  ```java
  public class XxxRequest {
      // 字段说明
  }
```
- **响应 DTO**：
  ```java
  public class XxxResponse {
      // 字段说明
  }
  ```
- **错误码**：
  | 错误码 | 说明 | HTTP 状态 |

## 分页规范
## 批量操作规范
